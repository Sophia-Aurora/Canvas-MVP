# 选区功能

1. 点击选区功能的时候currentTool值为select（d）
2. main29有定义了选区变量
3. main495行有复制元素函数

# 文本

1. 点击文本功能的时候currentTool值为text(已经定义好了)
2. canvas306行，重绘函数添加文本绘制函数名（u）
3. main220，文字数据结构

# 图片

1. 点击图片功能的时候currentTool值为image（d）
2. 添加图片文件的框看toolbars168到231（u）
3. main268，图片数据结构

# 画笔

1. 点击画笔功能的时候currentTool值为pen
2. canvas313行，重绘函数添加画笔绘制函数名
3. main322行，画笔数据结构

# 文件

1. 我改了toolbars第485行

# 整体

1. 绘制状态高亮框函数在canvas321行，后面应该删除
2. main90有一个获取元素边界函数，意义不明
3. 绘制工厂函数main383
