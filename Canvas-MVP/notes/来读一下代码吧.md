# toolbars

1. 获取dom
2. 给每个工具按钮添加监听
3. 初始化工具栏以及里面所有元素
4. 定义关闭侧边栏事件，给点击叉号添加监听，内部调用关闭所有侧边栏函数。

- 关闭侧边栏：移除所有工具按钮和侧边栏active，把当前工具和元素都设为null，侧边栏是否打开设为false

5. 处理工具按钮点击后出现侧边栏事件：

- 检查是不是重复点击已经active的按钮
- 移除所有按钮和侧边栏的active状态（被选中的后续再加，这样子逻辑清晰，不用额外记录哪一个按钮是active的）
- 如果重复点击：面板消失，重置工具和元素
- 如果不是重复点击：给按钮加active，按照index确定展示哪一个面板并调用展示函数

6. 处理面板展示事件

- 同时侧边栏是否打开设为true，禁止拖拽

7. 初始化文本对话框事件（还没看！！！！！！）

8. 调用颜色选择事件和形状选择事件函数

9. 给颜色添加点击事件

10. 给形状添加点击事件

- 用switch，根据索引值确定currentTool值（默认直线）

11. 边框，背景颜色处理事件：

- 点击的时候添加active
- 切换颜色作用位置的时候默认选中第一个（监听边框和背景对应的按钮）

12. 文字选项

- 下拉菜单：当文字面板出现且点击选择框时出现；当点击其他位置时消失（监听整个文档的点击事件，如果target不是选择框时，就关闭面板）
- 按钮点击：粗体和斜体

13. 画布操作事件初始化

- 给各个按钮监听并调用函数
- 生成唯一id

14. 创建画布元素

- 如果当前是临时画布（还没有名字），那就是保存当前画布。
- 如果当前不是临时画布，就创建新画布
- 关于向后端发送数据，注释里面写得很清楚了，在508行

15. 保存画布

- 检查是否临时画布

16. 打开画布列表

- 727行

17. 加载画布

- 750行

# canvas

1. 定义全局变量
2. ##### 转换坐标逻辑

- 鼠标位置减去画布位置：消除浏览器窗口的影响
- 结果减去拖动距离再除以缩放比：消除缩放和平移的影响

3. 初始化应用
4. 初始化鼠标拖拽事件

- 判断有没有选中工具，有没有打开面板，如果有才可以拖拽（拖拽记为true）
- 可以拖拽：记录当前鼠标相对于可视区位置，mousemove过程更新鼠标位置（用client），并在offset加上client的差值，然后更新鼠标位置，重绘画布

5. 缩放画布

- 获取鼠标在画布上的坐标
- 计算鼠标在画布上的实际位置（计算缩放中心）（这里后续优化可以调用计算函数）
- 计算新的缩放比例，并添加限制
- 调整偏移量：offset为屏幕坐标位置减去画布位置与新的缩放值的积
- 190行

6. ##### 绘制函数

- 清空画布
- 保存状态
- 全局视图变换（缩放，拖拽）
- 遍历数组，绘制所有元素

7. 绘制状态中高亮框

- 321行，这个要不要留呢

8. 初始化应用
9. 导出全局变量和初始化函数

- 不导出的话其他js文件import了这个文件也用不了这些变量和函数哦！

# main

1. 元素类型枚举
2. 创建元素数据类型
3. 工厂函数
4. 属性面板同步到元素
5. 元素同步到属性面板
6. 复制元素

# graph

1. 监听mousedown，判断当前工具，调用开始绘制函数
2. 监听mousemove，判断当前工具，调用绘制函数
3. 监听mouseup，正在绘制状态为假
4. 监听click，判断是否点击在画布空白区域，如果是的话取消当前元素的选中
